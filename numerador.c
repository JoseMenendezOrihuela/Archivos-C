#include <stdio.h>

float division(float numerador,float denominador)

float main(){
	float a=0;
	float b=0;
	resultado=0;
	division(a,b);
	
	scanf("%f", && numerador);
	printf("\f   Introduzca el numerador: " );
	scanf("%f", && denominador);
	printf("\f Introduzca el denominador: ");
	
}
